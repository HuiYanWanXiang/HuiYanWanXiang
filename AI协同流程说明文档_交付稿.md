# AI协同流程说明文档

项目名称：YiYanKeTang（易演课堂）  
队伍编号：［待填写］  
团队名称：［待填写］  
文档日期：2026年4月13日

---

## 一、项目概述

本项目面向中学数学与物理教学场景，建设了一个“文本需求到可视化教学内容”的智能生成系统。系统支持两类核心输出：

1. 交互式教学网页（HTML/Canvas/SVG）。
2. 演示动画视频（Manim渲染输出MP4）。

在协同机制上，项目采用“预设资源快速命中 + 大模型动态生成”的双轨策略：当用户输入可匹配到预设主题时，系统直接返回高质量现成资源；当输入为新主题或复杂需求时，系统进入AI生成链路，自动完成提示词增强、内容生成、任务调度、结果交付。

本项目在架构上兼容多种AI平台接口，当前主要使用DeepSeek兼容接口作为生产侧模型能力来源，同时保留切换其他兼容平台的配置能力，以保障系统的可持续演进和平台稳定性。

## 二、AI协同总体架构

系统协同流程可概括为“输入理解—策略路由—模型生成—结果校验—可视化交付”五个阶段。

1. 输入理解：前端接收教师或学生输入的教学主题与要求。
2. 策略路由：后端先执行预设匹配，命中则走快速返回，未命中则进入AI生成。
3. 模型生成：根据任务类型分别调用HTML生成链路或Manim视频生成链路。
4. 结果校验：对生成代码进行格式清洗、安全检查与结构补全。
5. 可视化交付：通过异步任务状态轮询，向前端返回可预览和可下载成果。

该架构解决了教育内容生成中的三个关键问题：

1. 速度问题：通过预设命中机制降低重复生成开销。
2. 质量问题：通过系统提示词和学科知识增强提升结果可用性。
3. 稳定问题：通过鉴权、配额、日志、异常处理和异步调度确保服务连续运行。

## 三、设计环节中的AI协同

### 3.1 需求分层与能力拆解

在设计阶段，我们将用户需求划分为标准化场景与开放式场景：

1. 标准化场景：如“勾股定理”“简谐振动”等高频教学主题，采用预设库直接提供页面和视频素材。
2. 开放式场景：如“自定义实验条件”“跨章节综合可视化”等个性化需求，采用大模型即时生成。

这种分层设计实现了“高频需求高效率、长尾需求高灵活性”的平衡。

### 3.2 提示词工程设计

系统采用“基础系统提示词 + 领域知识增强”的组合策略：

1. 基础提示词用于约束输出风格、结构规范和教学表达方式。
2. 领域知识增强模块根据关键词动态注入物理或数学知识片段。
3. 对生成网页明确要求中文表达、可交互结构、可运行可演示。

该设计显著降低了模型输出偏离教学目标的概率，提升了一次生成可用率。

### 3.3 多平台兼容设计

模型调用层按照兼容接口方式封装，核心配置由模型名、基础URL、密钥等变量驱动。由此可实现：

1. 当前使用DeepSeek服务。
2. 保留切换其他兼容平台的能力。
3. 在不大改业务代码的前提下快速迁移模型供应侧。

这一设计降低了平台单点依赖风险，提高了项目在竞赛和后续落地中的可持续性。

## 四、开发环节中的AI协同

### 4.1 前后端协同开发

前端负责输入、状态展示与结果渲染，后端负责任务调度、模型调用、权限校验和文件落盘。开发中采用异步接口和任务队列，保证生成任务不会阻塞主服务请求。

典型流程为：

1. 前端提交生成请求。
2. 后端立即返回任务ID并异步执行。
3. 前端定时轮询任务状态。
4. 任务完成后展示结果并支持下载。

该流程显著优化了交互体验，避免长耗时任务导致前端无响应。

### 4.2 HTML生成链路协同

HTML链路主要由以下步骤构成：

1. 基于用户输入构建系统提示词。
2. 组合领域知识增强内容。
3. 调用大模型生成网页代码。
4. 清理返回文本中的无关包裹标记。
5. 自动补全HTML收尾结构。
6. 存储生成文件并返回访问路径。

该链路解决了“生成代码可运行性与教学可读性”的双重目标。

### 4.3 Manim视频链路协同

视频链路由专门路由与渲染模块完成，流程包括：

1. 根据教学需求生成Manim脚本。
2. 对脚本进行安全检查（危险导入与调用拦截）。
3. 进入渲染流程生成MP4。
4. 回传状态和可播放地址。

其中，安全校验机制有效避免了模型生成代码直接执行带来的风险，保障了系统运行安全。

### 4.4 用户与配额协同机制

系统在开发阶段内置用户鉴权与配额控制：

1. 使用JWT进行登录态认证。
2. 普通用户按次数配额使用AI生成能力。
3. 管理用户具备更高权限与调试能力。

该机制解决了资源公平使用和成本可控问题，支撑系统在多用户条件下稳定运行。

## 五、测试环节中的AI协同

测试阶段重点围绕“可用性、稳定性、安全性、一致性”展开。

### 5.1 功能正确性测试

1. 提示词加载是否成功。
2. 关键目录与资源是否完整。
3. 预设匹配是否命中预期主题。
4. 任务状态流转是否正确（排队、运行、完成、失败）。

### 5.2 稳定性测试

1. 异步并发下任务是否互不干扰。
2. 生成失败时是否正确返回错误并保留日志。
3. 重复请求和边界输入下系统是否可持续响应。

### 5.3 安全性测试

1. 鉴权接口是否能拦截未授权访问。
2. 视频脚本安全规则是否能拦截危险代码。
3. 下载接口是否仅允许合法用户访问自身可用资源。

### 5.4 人工评审与闭环优化

除自动化测试外，团队还通过人工抽检方式评估生成内容的教学准确性、表达清晰度和交互可理解性，并将问题反哺到提示词与预设库中，形成持续优化闭环。

## 六、运营环节中的AI协同

### 6.1 运行监控与问题追踪

系统通过日志和错误记录机制追踪生成任务运行情况，便于快速定位问题来源。针对接口超时、模型返回异常、渲染失败等场景，均可在日志中快速回溯。

### 6.2 内容资产沉淀

每次生成的网页和视频可被保存为可复用资产，为后续课程制作提供素材积累。高质量结果可进一步固化为新预设，反向提升后续命中率和响应速度。

### 6.3 成本与服务平衡

运营阶段通过配额策略和权限分层，实现教学服务质量与模型调用成本之间的平衡，避免资源浪费，保障核心用户体验。

### 6.4 持续迭代机制

团队以“日志分析—问题归因—提示词优化—预设扩容—再次验证”的节奏进行迭代，使系统在真实使用中不断提升稳定性和教学价值。

## 七、AI协同解决的关键问题与成效

本项目通过AI协同流程，重点解决了以下问题：

1. 教学内容制作效率低：通过预设与自动生成，将制作周期从人工逐页编写大幅缩短到分钟级。
2. 内容风格不统一：通过系统提示词约束和知识增强，提升输出一致性。
3. 复杂流程难维护：通过模块化拆分与异步任务机制，提高可维护性与扩展性。
4. 安全与成本风险：通过鉴权、配额、安全检查与日志追踪，实现风险可控。

综合来看，AI协同流程不仅提升了开发效率，也提升了系统在教学场景中的实用性和可推广性。

## 八、后续优化方向

1. 引入更细粒度的学科知识路由，实现按知识点自动选择提示词模板。
2. 建立更完整的质量评估指标体系，如交互完整度、教学准确度、渲染成功率等。
3. 增强多模型调度策略，在不同任务类型下自动选择最优模型。
4. 完善运营看板，形成可视化的数据驱动迭代机制。

## 九、结语

本项目围绕教育内容生成这一典型场景，构建了覆盖设计、开发、测试、运营全链路的AI协同流程。通过“预设快速命中 + 大模型动态生成 + 工程化治理”三位一体方案，系统实现了效率、质量与稳定性的统一。

在平台策略上，项目已具备多平台兼容能力，当前以DeepSeek为主要模型能力来源。该方案具备较好的可复制性和可扩展性，可为智慧教育场景中的数字内容生产提供实践参考。

---

## 附：排版与提交执行清单（用于Word）

1. 页面设置：A4纸。
2. 页边距：上2cm、下2cm、左2cm、右2cm。
3. 装订线：1cm。
4. 正文字体：仿宋，四号。
5. 行距：单倍行距。
6. 段落：段前0.5行，段后0.5行。
7. 文件命名：队伍编号-团队名称-C类***AI协同流程说明文档.docx。
8. 导出要求：由docx导出为PDF，单文件不超过20MB。

> 使用说明：将文中“［待填写］”替换为真实信息后，按以上参数在Word中统一排版并导出PDF即可提交。